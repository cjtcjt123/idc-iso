# Expo / React Native
node_modules/
.expo/
dist/
web-build/

# Native build artifacts (generated by `expo prebuild` inside CI)
ios/
ios/Pods/
ios/build/
android/
android/build/
android/app/build/

# CI export / secrets
exportOptions.plist
*.p12
*.mobileprovision

# Local env / secrets
.env
.env.local
*.local

# macOS
.DS_Store
