# Expo / React Native
node_modules/
.expo/
dist/
web-build/

# Native build artifacts (generated by `expo prebuild` inside CI)
ios/
ios/Pods/
ios/build/
android/
android/build/
android/app/build/

# CI export / secrets
exportOptions.plist
*.p12
*.mobileprovision

# Local env / secrets
.env
.env.local
*.local

# macOS
.DS_Store

# 本地 Expo web 预览辅助（沙箱降级 preload + 隔离 HOME，勿提交）
.expopatch.cjs
.exphome/
proxy.cjs
